/** @type {import('next').Config} */
const tailwindConfig = {
  theme: {
    extend: {
      colors: {
        brand: {
          orange: "#ff5e00",
          dark: "#141414",
        },
      },
    },
  },
};

export default tailwindConfig;